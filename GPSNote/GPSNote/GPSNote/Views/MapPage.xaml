<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GPSNote.Views.MapPage"
             xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps" 
             xmlns:custommap="clr-namespace:GPSNote.CustomControls.CustomMap"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightPageBackgroundColor}, Dark={StaticResource DarkPageBackgroundColor}}"
             Title="{Binding Resources[MapTitle]}">

    <Grid >
        <Button Text="Satellite" Grid.Row="0" Command="{Binding ChangeMapTypeCommand}"></Button>

        <custommap:CustomMapView  Grid.Row="1" MapType="{Binding type}" Pins ="{Binding pins}">
        </custommap:CustomMapView>
        
    </Grid>

</ContentPage>